<%# Needs: contentSection = ContentSection%>

<div id="id-content">
    <% contentSections.forEach( function (contentSection) { %>
        <%- include('content-section/contentSection', {contentSection: contentSection}) %>
    <% }) %>
</div>
